# Future agent definitions

No specialized agents are created or enabled by this foundation.

Future definitions must enter through `../AGENT-BOOTSTRAP.md` and specify only: Role, Responsibilities, Allowed actions, Forbidden actions, Required knowledge, Inputs, Outputs, Handoff rules, and Quality gates.

Reference the shared knowledge; do not copy it or introduce competing global rules. Creating an agent requires a subsequent explicit task.
